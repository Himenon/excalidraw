pre-commit:
  parallel: true
  jobs:
    - name: format-check
      run: pnpm oxfmt --check {staged_files}
      glob: "*.{ts,tsx,js,jsx,json,css,scss,md,html,yml}"

    - name: lint
      run: pnpm oxlint --react-plugin --import-plugin {staged_files}
      glob: "*.{ts,tsx,js}"
